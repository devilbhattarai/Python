import random
class Dice:
    def roll(self):
        first=random.randint(1,6)
        second = random.randint(1,6)
        return first,second
    
dice=Dice()
a=dice.roll()
print(a)

Friday day off hahahahaha
Need to keep the streak on